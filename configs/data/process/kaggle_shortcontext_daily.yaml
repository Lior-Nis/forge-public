defaults:
  - _self_

sampling_rate: 100

lengths:
  block_len: 200
  stride_len: 200

any_fog_labeling: false
data_type: unlabeled

zarr_config:
  chunk_size: 256
  compressor: lz4
  compression_level: 1

processor:
  _target_: data.process.processor.unlabeled.UnlabeledDataProcessor
