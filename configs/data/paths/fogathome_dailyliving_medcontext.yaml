# Paths config for medcontext (500-timestep) fogathome daily-living labeled held-out eval set.
# NOTE: fogathome_dailyliving is labeled and held-out — NOT pretraining data.

defaults:
  - base
  - _self_

input_dir: ${.raw_root}/fogathome_dailyliving
dataset_path: len500_stride200_fogstride100_anyfog_fogathome_dailyliving.zarr
