# Paths config for medcontext (500-timestep) kaggle defog data generation.
# Used with: python -m data.process paths=kaggle_defog_medcontext process=kaggle_medcontext

defaults:
  - base
  - _self_

input_dir: ${.raw_root}/kaggle_labeled
dataset_path: len500_stride200_fogstride100_anyfog_kaggle_defog.zarr
