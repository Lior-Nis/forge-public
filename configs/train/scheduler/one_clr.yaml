_target_: torch.optim.lr_scheduler.OneCycleLR
max_lr: ${train.optimizer.lr}
epochs: ${train.trainer.max_epochs}
steps_per_epoch: ${train.trainer.limit_train_batches}
pct_start: 0.3
div_factor: 25.0
final_div_factor: 1000.0
anneal_strategy: "cos"
three_phase: false
