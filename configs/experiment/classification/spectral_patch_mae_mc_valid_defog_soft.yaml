# @package _global_
#
# Soft-label probe/finetune on defog using SpectralPatchEncoder pretrained with
# medcontext MAE. Uses fog_ratio labeling: BCEWithLogitsLoss with fog_ratio
# float target instead of binary cross-entropy.
#
# Context: 500-step (5s), time_patch=50, 10 temporal tokens, vit_depth=4.

defaults:
  - /data/hierarchical_norm@data.hierarchical_norm: cross_protocol
  - override /model/backbone: spectral_patch_encoder
  - override /data/process: kaggle_medcontext
  - override /data/paths: kaggle_defog_valid_medcontext
  - override /data/splits: kaggle_labeled/defog_only
  - override /model/signal_augmentor: default
  - override /model/preprocessors: hierarchical_norm_pure
  - override /model/head: gru_binary_classification_heavy
  - override /train/optimizer: adamw
  - override /train/scheduler: cosine
  - override /train/loss: fog_ratio
  - override /train/callbacks: classification
  - override /train/registry: default
  - override /train/trainer: fog_trainer_no_limits
  - override /train/logger: classification
  - override /data/dataset: fog_ratio_dataset
  - override /data/dataloader: classification_loader
  - _self_

global:
  experiment_name: spectral_patch_mae_mc_valid_defog_soft
  seed: 42

model:
  img_size: ${global.img_size}
  transform:
    _target_: model.transforms.WaveletTransform
    wavelet: 'morl'
    min_freq: 0.3
    max_freq: 50.0
    n_scales: 100
    highlight_transient: false
    transient_weight: 0.0
    img_size: ${global.img_size}
    interpolate: false
  backbone:
    dropout: 0.0
  head:
    input_seq_len: 10   # 500 / time_patch=50 = 10 temporal tokens
    dropout: 0.3

train:
  weights:
    load_from: null
    load_strategy: backbone_only
    freeze_backbone: true
    unfreeze_schedule:
      enabled: false

  optimizer:
    lr: 1e-3

  scheduler:
    T_max: 30

  trainer:
    max_epochs: 30
