# @package model

defaults:
  - preprocessors: hierarchical_norm_pure
  - signal_augmentor: default
  - transform: wavelet
  - spectral_augmentor: null
  - backbone: fogformer
  - head: gru_binary_classification_light
  - _self_

img_size: ${global.img_size}
