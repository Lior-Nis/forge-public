# @package model

defaults:
  - preprocessors: minimal
  - signal_augmentor: null
  - transform: raw
  - spectral_augmentor: null
  - backbone: fogformer
  - head: mae_decoder
  - _self_

mask_ratio: 0.5
mask_mode: "temporal"
